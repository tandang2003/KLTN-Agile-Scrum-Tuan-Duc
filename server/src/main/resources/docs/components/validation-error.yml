type: object
required:
  - statusCode
properties:
  statusCode:
    type: integer
  error:
    type: array
    items:
      type: object
      properties:
        field:
          type: string
          description: "field in body is error"
        message:
          type: string
          description: "message from server"
  message:
    type: string
