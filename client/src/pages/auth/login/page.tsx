import LoginForm from '@/components/form/LoginForm'

const LoginPage = () => {
  return (
    <LoginForm className='animate-fade-down animate-once animation-duration-1000 animate-ease-linear' />
  )
}

export default LoginPage
