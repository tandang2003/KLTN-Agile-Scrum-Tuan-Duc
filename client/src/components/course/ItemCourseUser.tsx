import { UserCourseResponseType } from '@/types/course.type'

type CourseItemProps = {
  data: UserCourseResponseType
}

const CourseUserItem = ({}: CourseItemProps) => {
  return <div></div>
}

export default CourseUserItem
