import ListView from '@/components/ListView'
import { Button } from '@/components/ui/button'
import { SubTask } from '@/types/model/issue.model'

const CreateSubTask = ({}) => {
  const handleClick = () => {}

  return (
    <>
      <Button onClick={handleClick}>SubTask</Button>
    </>
  )
}

export default CreateSubTask
